# 项目工作规则

## 用户与交付默认假设

1. 默认假设用户完全不懂代码、命令行、PowerShell、依赖安装和本地服务器。
2. 除非涉及账号登录、授权、付费或不可替代的人工操作，否则不要要求用户执行终端命令。
3. 需要运行命令、安装依赖、启动服务、构建项目、查看日志时，由 Codex 自行完成。
4. 用户主要通过自然语言描述玩法、界面、美术和体验需求。先准确理解需求，不要擅自增加大型系统或扩大需求范围。
5. 只有涉及玩法取舍、表现方向或产品决策时才向用户提问；技术实现方式由 Codex 自行选择稳妥方案。
6. 不要只说“代码已经完成”。必须交付用户能够直接打开、点击和体验的结果。
7. 每次交付必须使用非技术语言说明：本次修改了什么、用户应该点击哪里验收、验收结果、在线预览链接、可下载文件、已知问题。

## 安全修改

1. 每次修改前保存可回滚版本，不得破坏已有功能、已有资源和已有玩家存档。
2. 优先使用最小改动；未经明确要求，不要整体重构项目、替换技术框架或升级主要依赖。
3. 修改前检查影响范围和存档兼容性；修改后执行回归测试。
4. 出现错误时，先自行阅读错误日志、修复并重新测试，不要立即把错误信息丢给用户处理。

## 自动验证

1. 每次修改后必须自动运行项目，并验证核心流程是否正常。
2. 游戏以 H5 和移动端体验为优先，重点检查触摸操作、界面遮挡、屏幕适配、资源加载和本地存档。
3. 自动验收至少覆盖：页面打开、新游戏、继续游戏、存档、资源产出、核心建筑操作、移动端显示、按钮点击和控制台错误。
4. 若测试失败，先修复并重新测试；不能让损坏版本覆盖稳定版本。

## 版更确认

每个新版本开工前，先整理版更明细（见 VERSION_PLANS.md）给用户确认，用户确认后才允许写代码；确认范围之外不扩需求。

## 版本验收明细

每次提交新版本代码包时，必须同时提供一份可逐条核验的版本验收明细。明细需包含验收步骤、预期结果和结果记录方式；用户完成验收前，不得把该版本标记为“已验收”。

## 死循环预警（用户指令，2026-07-29 起长期有效）

如果发现自己陷入重复循环（反复读取同一内容、反复执行同类命令而无产出、长时间没有实际进展导致 tokens 浪费），必须：
1. 自动停止当前工作；
2. 立即向用户发出预警，说明“疑似陷入死循环/无产出，已停止”；
3. 不要继续硬撑或重复尝试，等用户重新安排工作。
